# Vercel KV - auto-populated when you link KV store in Vercel dashboard
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Secret to protect the /api/ingest endpoint
# Set this in Vercel env vars AND in your shortcut's Authorization header
INGEST_SECRET=your-secret-here
